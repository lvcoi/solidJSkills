# SolidJS Control Flow

## Preferred primitives

- `<Show>` for conditional rendering.
- `<For>` for list rendering with item-level reactivity.
- `<Switch>/<Match>` for branch sets.

## Guidance

- Keep branch logic declarative in JSX.
- Avoid ad-hoc ternary nests for complex states.
